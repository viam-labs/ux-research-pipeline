# Known Issues — Skip or Deprioritize These

Add issues here that are already being worked on, already decided, or 
otherwise shouldn't generate new tickets. The LLM will see these and 
avoid filing duplicates.

Format: one issue per line, brief description of why to skip.

---

- Example: Save button discoverability — already being redesigned
- Example: Component status visibility after setup — active solution exploration in progress
